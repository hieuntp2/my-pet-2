# Pet Eye Animation Source Assets

Place user-provided source sprite sheets in this folder.

Required for the first Android milestone:

```text
looking-idle.png
looking-blink.png
```

Codex must copy these PNG files into:

```text
app/src/main/assets/pet/eyes/
```

Do not rename the files when using Android `assets/`.
